/*	
 *------ data structure ----------
 * 1.  skiplist
 * 2.  hash_table
 * 3.  binary_tree
 * 4.  avl_tree
 * 5.  binary_search_tree
 * 6.  heap(min or max)
 * 7.  array(search data in O(1), from zeroMQ)
 * 8.  deque
 * 9.  rb_tree
 * 10. ref_count data structure
 * 11. map_reduce
 * 12. bloom_filter
 * 13. zset(from redis use hash_table(for O(1) find) with skiplist(for sequence))
 * 14. varint(from redis)
 * 15. iterator(items & cur_index)  ---->> 才有接口与实现分离的策略
 * 16. LRU cache
 * 17. radix tree
 * 18. nginx 带前缀后缀匹配的hash_table
 *
 *
 *
 *------ algrithmn ----------
 * 1.  贪心
 * 2.  图算法
 * 3.  树的相关算法
 * 4.  动态规划
 * 5.  排序算法
 * 6.  搜索算法 
 * 7.  
 *
 *
 *
 *
 *
 * /
